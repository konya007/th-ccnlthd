import { StyleSheet } from "react-native";

const homeStyle = StyleSheet.create({
    container: {
        flex: 1,
        padding: 10,
    },
    h1: {
        fontSize: 20,
        fontWeight: "bold"
    }
})

export {homeStyle}